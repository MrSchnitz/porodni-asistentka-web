# SSL/HTTPS Configuration Override
# This file extends docker-compose.yml with SSL/HTTPS support
# Usage: docker compose -f docker-compose.yml -f docker-compose.ssl.yml up -d
#
# This file is generated by setup-ssl.sh
# To set up SSL, run: ./other-scripts/setup-ssl.sh yourdomain.com your-email@example.com
#
# Before using this file:
# 1. Run setup-ssl.sh to generate this file with your domain and email
# 2. Make sure your domain DNS points to this server
# 3. Ensure ports 80 and 443 are open in your firewall
#
# NOTE: This is a template. Run setup-ssl.sh to generate the actual file with your values.

